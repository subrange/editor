void putchar(int c);

int main() {
    char *msg = "A";  // Single character string
    
    // For now, just test that we can get the address
    // We'll print something to show it compiled
    putchar(65); // 'A'
    
    return 0;
}