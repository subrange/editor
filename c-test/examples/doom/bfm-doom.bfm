#include "lib/program.bfm"

@program(
  @leaf(putchar,
    @a({:store s0, r0, r0})
  )

  @proc(add,
    {:add sc, } @arg1 {:, } @arg2 {br}
    @retval(sc, r0)
  )

  @proc(main,
    @call1(putchar, @imm, @asciiz("A"))
    @call2(add, @imm, 5, @imm, 10)
    @call2(add, @reg, rv0, @reg, rv0)
  )
)